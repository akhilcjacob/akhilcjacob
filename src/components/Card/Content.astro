---
interface Props {
  title?: string;
  body?: string;
}

const { title, body } = Astro.props;
---

<>
  {title && <h2 class="text-xl font-bold mb-4 text-white/80">{title}</h2>}
  {body && <p class="text-base text-white/60">{body}</p>}
  <slot />
</>
